#there are two type of list homogeneous and heterogeneous list 
#single element in list
list =[12]

# dictionary 
student={"name"="vivek","age"=21}
student["age"]=21 #age updated to 21

#2 methods to get value of dictionary 
1. dictionary_name[key]
2. dictionary_name.get(key)

#dictionary methods
here dict = dictionary_name
1. .dict()
2. dict.key()
3. dict.value()
4. dict.items()
5. dict.copy()

zip function-> 
in python zip function used to combine multiple iterables(list,tuples) to tuples


student=["vivek","vikash","aman"]
age=[20,25,18]
combined = zip(student,age)

#create dictionary from two list
combined_list= dict(zip(student,age))



#set does not support indexing
student.add("adarsh")
student.remove("vikash")
#regular set created by {} or set() but frozenset() create for immutable set which can used as key in dictionary 

#tuples are immutable,ordered,accept duplicate,immutable 
# methods in tuples
1. s.len()
2. s.max()
3. s.min()

# single element list using tuple
single_element = (5,) #commma inside parenthesis is mandatory 

os.path.exists(file_path)
line.strip() # remove all newlines in file
